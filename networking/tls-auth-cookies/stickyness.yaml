# app-deploy.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: secure-app-deploy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: secure-app
  template:
    metadata:
      labels:
        app: secure-app
    spec:
      containers:
      - name: http-echo
        image: hashicorp/http-echo
        args: ["-text=Welcome to the Secret Vault !!", "-listen=:80"]
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: secure-app-svc
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: secure-app
---
# ingress-secure.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: secure-app-ingress
  annotations:
    # ─── BASIC AUTH CONFIGURATION ────────────────────────────────────
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: secure-app-auth
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required - Secure Vault"

    # ─── SESSION STICKINESS CONFIGURATION ────────────────────────────
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/session-cookie-name: "route-cookie"
    nginx.ingress.kubernetes.io/affinity-mode: "balanced"
    
    # Sets cookie lifespan to 5 minutes (300 seconds)
    nginx.ingress.kubernetes.io/session-cookie-expires: "300"
    
    # Custom path restriction for the cookie
    nginx.ingress.kubernetes.io/session-cookie-path: "/"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - secure-app.com
    secretName: my-app-tls-secret
  rules:
  - host: secure-app.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: secure-app-svc
            port:
              number: 80
