apiVersion: v1
kind: Service
metadata:
  name: my-headless-service
spec:
  clusterIP: None  # 👈 This is the magic switch that makes it headless
  selector:
    app: web-headless
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
